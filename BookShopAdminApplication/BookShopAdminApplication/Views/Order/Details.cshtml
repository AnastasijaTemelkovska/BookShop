@*
    For more information on enabling MVC for empty projects, visit https://go.microsoft.com/fwlink/?LinkID=397860
*@
@{
}
@model BookShopAdminApplication.Models.Order


<div class="container">
    <div>
        <a class="btn btn-success">Order for: @Model.User.UserName</a>
    </div>

    @for (int i = 0; i < Model.Books.Count; i++)
    {
        var item = Model.Books.ElementAt(i).SelectedBook;
        if (i % 3 == 0)
        {
            @:<div class="row">
            }
            <div class="col-md-4 m-3">
                <div class="card" style="width: 20rem; height: 55rem;">
                    <img class="card-img-top" src="@item.BookImage" alt="Image for book" style="height=50%" />

                    <div class="card-body">
                        <h3 class="card-title">@item.BookName</h3>
                        <p class="card-text">@item.Author</p>
                        <p class="card-text">@item.BookDescription</p>
                        <h6>Price: @item.BookPrice</h6>
                    </div>


                </div>
            </div>
            if (i % 3 == 2)
            {
            @:</div>
        }
    }

</div>
